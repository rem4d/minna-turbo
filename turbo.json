{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": [".env"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "dependsOn": ["^build"],
      "outputs": [".cache/tsbuildinfo.json", "dist/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "batch": {
      "persistent": true,
      "cache": false
    },
    "py": {
      "persistent": true,
      "cache": false
    },
    "clean": {
      "cache": false
    },
    "format": {
      "outputs": [".cache/.prettiercache"],
      "outputLogs": "new-only"
    },
    "lint": {
      "dependsOn": ["^build"],
      "outputs": [".cache/.eslintcache"]
    },
    "check-types": {
      "dependsOn": ["^build", "^check-types"]
    },
    "test": {
      "dependsOn": ["^test"]
    },
    "//#test:watch": {
      "cache": false,
      "persistent": true
    }
  },
  "globalEnv": [
    "SUPABASE_ANON_KEY",
    "SUPABASE_URL",
    "OPENAI_API_KEY",
    "PORT",
    "HOST",
    "VITE_API_SERVER",
    "VITE_ROLLBAR_API_TOKEN"
  ],
  "globalPassThroughEnv": ["NODE_ENV"],
  "env": {
    "VITE_ROLLBAR_API_TOKEN": "required",
    "DEV": "optional"
  }
}
